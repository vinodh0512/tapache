# Server Configuration
PORT=5000
HOST=0.0.0.0

# MongoDB Atlas Cloud Synchronization
# Set your MongoDB Atlas connection string below:
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.uph7bbi.mongodb.net/tepache_fermentation?retryWrites=true&w=majority

# Cloud sync interval in milliseconds (default: 60000 = 1 minute)
MONGODB_SYNC_INTERVAL_MS=60000
